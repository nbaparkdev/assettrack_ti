# ============================================================
# AssetTrack TI - Requirements
# Última revisão: 2026-06-27 (Módulo de Compras completo)
# ============================================================

# --- Core Framework ---
fastapi==0.115.0
uvicorn[standard]==0.31.0

# --- Database (async SQLAlchemy + drivers) ---
sqlalchemy==2.0.35
alembic==1.13.3
asyncpg==0.29.0        # Driver async para PostgreSQL (produção)
aiosqlite==0.20.0      # Driver async para SQLite (dev/local)

# --- Validation & Settings ---
pydantic==2.9.2
pydantic-settings==2.5.2

# --- Auth & Security ---
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1

# --- Web / Forms ---
python-multipart==0.0.9  # Upload de arquivos (contratos PDF, fotos, NF-e)
email-validator==2.2.0

# --- QR Code (Scanner de Patrimônio / Usuários) ---
qrcode[pil]==7.4.2
pyzbar==0.1.9
pillow==10.4.0

# --- Templates ---
jinja2>=3.1.2

# --- Utilities ---
python-dotenv==1.0.1
httpx==0.27.2
tzdata                   # Suporte a timezone (America/Sao_Paulo)

# --- Rate Limiting ---
slowapi>=0.1.9

# --- PDF Generation (Relatórios) ---
weasyprint>=68.0

# --- Testing ---
pytest==8.3.3
pytest-asyncio>=1.3.0

# NOTA: csv, io, os, pathlib são módulos da stdlib Python (sem instalação necessária)
